services:
  omnitool:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_SUPABASE_URL: ${NEXT_PUBLIC_SUPABASE_URL}
        NEXT_PUBLIC_SUPABASE_ANON_KEY: ${NEXT_PUBLIC_SUPABASE_ANON_KEY}
        NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    depends_on:
      - whatsapp

  whatsapp:
    build:
      context: ./whatsapp-service
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    environment:
      - SUPABASE_URL=${NEXT_PUBLIC_SUPABASE_URL}
      - SUPABASE_SERVICE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - PORT=3001
      - NEXTJS_APP_URL=${NEXT_PUBLIC_APP_URL}
      - WEBHOOK_SECRET=${WHATSAPP_SERVICE_SECRET}
    volumes:
      - whatsapp_sessions:/app/.wwebjs_auth
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3001/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  cron:
    image: alpine
    restart: unless-stopped
    command: >
      sh -c "apk add --no-cache wget &&
        while true; do
          wget -q -O /dev/null --post-data '' \
            --header='Authorization: Bearer '\"$$CRON_SECRET\"'' \
            http://omnitool:3000/api/cron/broadcasts || true;
          sleep 60;
        done"
    environment:
      - CRON_SECRET=${CRON_SECRET}
    depends_on:
      - omnitool

volumes:
  whatsapp_sessions:
